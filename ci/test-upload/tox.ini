[tox]
envlist = py
skipsdist = True

[testenv]
deps =
    pytest
commands_pre =
    pip install --index-url https://test.pypi.org/simple/ tkfplaygroundpy==0.0.2.dev4
commands =
    py.test \
        --pyargs tkfplaygroundpy \
        {posargs}

[pytest]
addopts =
    --doctest-modules
