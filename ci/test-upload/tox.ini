[tox]
envlist = py
indexserver =
    test = https://test.pypi.org/simple/
skipsdist = True

[testenv]
deps =
    pytest
    :test:tkfplaygroundpy
pip_pre = true
commands =
    py.test \
        --pyargs tkfplaygroundpy \
        {posargs}

[pytest]
addopts =
    --doctest-modules
